name: module_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  weather:
    path: packages/weather
  pokedex:
    path: packages/pokedex


  cupertino_icons: ^1.0.2
  http: ^0.13.3
  firebase_remote_config: ^0.10.0
  firebase_core: ^1.2.0
  rxdart: ^0.27.1
  just_audio: ^0.7.5

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

  assets:
    - assets/

  fonts:
    - family: OpenSans
      fonts:
        - asset: fonts/OpenSans-Regular.ttf
